apiVersion: apps/v1
kind: Deployment
metadata:
  name: feedback-service
  namespace: kitchen-planner
  labels:
    app: feedback-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: feedback-service
  template:
    metadata:
      labels:
        app: feedback-service
    spec:
      containers:
      - name: feedback-service
        image: gcr.io/YOUR_GCP_PROJECT_ID/feedback-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8004
        envFrom:
        - configMapRef:
            name: kitchen-planner-config
        - secretRef:
            name: kitchen-planner-secrets
        readinessProbe:
          httpGet:
            path: /health
            port: 8004
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8004
          initialDelaySeconds: 15
          periodSeconds: 20
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: feedback-service
  namespace: kitchen-planner
spec:
  selector:
    app: feedback-service
  ports:
  - port: 8004
    targetPort: 8004
  type: ClusterIP
