apiVersion: apps/v1
kind: Deployment
metadata:
  name: translation-service
  namespace: kitchen-planner
  labels:
    app: translation-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: translation-service
  template:
    metadata:
      labels:
        app: translation-service
    spec:
      containers:
      - name: translation-service
        image: gcr.io/YOUR_GCP_PROJECT_ID/translation-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8003
        envFrom:
        - configMapRef:
            name: kitchen-planner-config
        - secretRef:
            name: kitchen-planner-secrets
        readinessProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 15
          periodSeconds: 20
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: translation-service
  namespace: kitchen-planner
spec:
  selector:
    app: translation-service
  ports:
  - port: 8003
    targetPort: 8003
  type: ClusterIP
